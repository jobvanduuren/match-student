<h1>New Student</h1>

<%= form_for @student do |f| %>

  <ul>
    <% @student.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>

  <%= f.label :name %>
  <%= f.text_field :name %>


  <%= f.submit "Create" %>
<% end %>
